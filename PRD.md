# PRD — 강남3구 아파트 실거래가 대시보드 (v1)

| 항목 | 내용 |
|---|---|
| 문서 버전 | 1.0 |
| 작성일 | 2026-08-12 |
| 산출물 | `dashboard.html` (+ `data/*.json`, `scripts/build_data.py`) |
| 배포 | GitHub Pages (정적) |

---

## 1. 개요 / 목표

서울 강남3구(강남구·서초구·송파구)의 최근 12개월(2025-07 ~ 2026-06) 아파트 **매매** 실거래 데이터 9,754건을 하나의 페이지에서 탐색할 수 있는 인터랙티브 대시보드를 만든다. 목적은 실서비스가 아니라 **포트폴리오/과제용 데모**로, "원본 CSV → 데이터 가공 → 웹 시각화 → 배포"까지의 전 과정을 수행할 수 있음을 보여주는 것이다. 빌드 툴체인 없이 순수 HTML/CSS/JS로 구현하며, 구 토글·단지 검색·정렬 등 클라이언트 사이드 인터랙션을 통해 정적 리포트가 아닌 "탐색 가능한 도구"임을 드러낸다.

---

## 2. 타겟 사용자 및 사용 시나리오

**주 사용자**: 포트폴리오를 검토하는 채용 담당자/멘토 (데스크톱 브라우저, 체류 1~3분)
**부 사용자**: 강남3구 시세를 훑어보려는 일반 방문자

| # | 시나리오 | 흐름 |
|---|---|---|
| S1 | 3구 시세를 한눈에 비교 | 진입 → 상단 KPI 카드에서 구별 평균가/거래량 즉시 확인 |
| S2 | 특정 구만 보고 싶음 | 구 토글에서 `서초구`만 켜기 → 차트·랭킹·테이블이 즉시 갱신 |
| S3 | 아는 단지 시세 확인 | 검색창에 "래미안" 입력 → 해당 단지 거래 내역만 테이블에 표시 |
| S4 | 최근 1년 시장 흐름 파악 | 월별 평균가 추이 선그래프의 기울기와 구간별 격차 확인 |
| S5 | 고가 거래 확인 | 랭킹 테이블을 거래금액 내림차순으로 정렬해 상위 거래 확인 |

---

## 3. 정보 구조 (페이지 구성)

단일 페이지(`dashboard.html`), 위에서 아래로 스크롤하는 1열 흐름. 섹션 순서는 **요약 → 추이 → 상세**.

```
┌ Header ─────────────────────────────────────────────┐
│ 타이틀 / 부제(데이터 기간·건수) / 원본 데이터 출처 표기 │
├ ① Global Control Bar (sticky) ──────────────────────┤
│ [강남구] [서초구] [송파구] 토글  |  🔍 단지/동 검색   │
├ ② KPI 요약 카드 (3~4장, grid) ──────────────────────┤
│ 전체 평균가 / 총 거래량 / 평균 평당가 / 기간          │
├ ③ 자치구별 비교 ────────────────────────────────────┤
│ 좌: 구별 평균 매매가 막대  |  우: 구별 거래량 막대     │
├ ④ 월별 평균가 추이 (선그래프) ──────────────────────┤
│ 선택된 구별 1개 라인 (최대 3개 라인) + 범례          │
├ ⑤ 실거래 랭킹 / 검색 테이블 ────────────────────────┤
│ 정렬 가능한 컬럼 헤더 + 페이지네이션(또는 더보기)     │
├ Footer ─────────────────────────────────────────────┤
│ 데이터 기준일, 제작자, GitHub 링크                    │
└─────────────────────────────────────────────────────┘
```

**핵심 원칙**: ①의 구 토글과 검색어는 **전역 상태**이며, ②③④⑤ 모든 섹션이 이 상태를 구독해 동시에 갱신된다.

---

## 4. 기능 요구사항

### F1. 전역 컨트롤 바 (sticky)

- **구 토글**: `강남구 / 서초구 / 송파구` 3개 pill 버튼. 다중 선택(체크박스 성격), 기본값은 3개 모두 ON.
  - 선택 상태: 배경 = 구별 시리즈 컬러, 텍스트 흰색. 비선택: 흰 배경 + 회색 테두리 + 회색 텍스트.
  - **최소 1개는 반드시 선택**되어야 함. 마지막 남은 1개를 끄려 하면 무시(토글 미동작).
  - 토글 시 갱신 대상: ② KPI 재계산, ③ 막대차트 바 표시/숨김, ④ 라인차트 해당 라인 표시/숨김, ⑤ 테이블 행 필터링. 전환은 200ms 이내 체감(즉시 재렌더).
- **검색창**: placeholder `단지명 또는 동 이름 검색 (예: 래미안, 반포동)`
  - 검색 대상 필드: `complex`(단지명), `dong`(법정동). 두 필드 중 하나라도 부분 일치하면 매치.
  - 대소문자 무시, 공백 제거 후 `includes` 부분 일치. 디바운스 200ms.
  - 검색어는 **⑤ 테이블에만** 적용(②③④는 구 토글만 반영). 단, 검색 결과 건수를 테이블 헤더에 `"래미안" 검색 결과 342건`으로 표기.
  - 우측에 `✕` 초기화 버튼(검색어 있을 때만 노출).
- **결과 0건 처리**: 테이블 영역에 빈 상태 메시지 `검색 결과가 없습니다. 다른 단지명을 입력해 보세요.`

### F2. KPI 요약 카드 (선택된 구 기준으로 재계산)

| 카드 | 값 | 보조 텍스트 |
|---|---|---|
| 평균 매매가 | 선택 구 전체 평균 `price` → `27.4억` 형식 | `n건 기준` |
| 총 거래량 | 선택 구 거래 건수 → `9,754건` | `2025.07 ~ 2026.06` |
| 평균 평당가 | 평균 `price_per_pyeong` → `1.2억/평` (만원 → 억 변환) | — |
| 최고가 거래 | 선택 구 내 최고 `price` | `단지명 · 전용 84.8㎡` |

- 금액 표기 규칙: 10,000만원 이상 → `X.X억`(소수 1자리), 미만 → `X,XXX만원`. 천단위 콤마 필수.

### F3. 자치구별 비교 (막대차트 2종, 가로 배치)

- **좌측 — 구별 평균 매매가**: x축 = 구, y축 = 평균가(억). 막대 위에 값 라벨 표시.
- **우측 — 구별 거래량**: x축 = 구, y축 = 건수.
- 선택 해제된 구의 막대는 차트에서 제거(축도 함께 재조정).
- 막대 색상 = 구별 시리즈 컬러(§6 참조). hover 시 툴팁으로 정확한 수치 노출.

### F4. 월별 평균가 추이 (선그래프)

- x축 = `contract_ym` 12개 포인트(`2025-07` ~ `2026-06`, `25.07` 형식으로 축약 표기).
- y축 = 해당 월 평균 매매가(억 단위). y축은 0부터 시작하지 않고 데이터 min/max에 여백 10%를 준 범위 사용(추세 가독성).
- 선택된 구마다 1개 라인(최대 3개). 라인 두께 2.5px, 데이터 포인트 원형 마커(r=3, hover 시 r=5).
- 툴팁: `2026-03 · 강남구 / 평균 28.1억 / 812건`.
- 범례는 차트 상단 우측, 구별 컬러 도트 + 라벨.

### F5. 실거래 랭킹 / 검색 테이블

- **표시 컬럼** (좌→우):
  | 컬럼 | 원본 필드 | 표기 |
  |---|---|---|
  | 계약일 | `contract_date` | `2026-03-14` |
  | 자치구 | `gu` | 컬러 뱃지 |
  | 동 | `dong` | 텍스트 |
  | 단지명 | `complex` | 검색어 하이라이트(`<mark>`) |
  | 전용면적 | `area_m2` | `84.8㎡` |
  | 층 | `floor` | `12층` |
  | 거래금액 | `price` | `36.9억` (우측 정렬, 강조 굵기) |
  | 평당가 | `price_per_pyeong` | `1.44억` (우측 정렬) |
- **기본 정렬**: 거래금액 내림차순 (= "랭킹" 성격).
- **정렬 가능 컬럼**: 계약일, 전용면적, 거래금액, 평당가. 헤더 클릭 시 desc → asc 토글, 활성 컬럼에 ▲/▼ 아이콘.
- **페이징**: 한 번에 50행 렌더, 하단 `더 보기(+50)` 버튼. 필터/정렬 변경 시 50행으로 리셋. (DOM 과다 생성 방지)
- **행 hover**: 배경 `#F8FAFC`로 변경.

### F6. 로딩 / 에러 상태

- 데이터 fetch 중: 각 차트/테이블 자리에 스켈레톤(회색 박스 + shimmer).
- fetch 실패 시: `데이터를 불러오지 못했습니다. 새로고침해 주세요.` + 재시도 버튼.

---

## 5. 데이터 요구사항

### 5.1 방식 결정: **사전 집계 JSON 방식 채택**

27MB 원본 CSV를 브라우저에서 fetch·파싱하는 것은 모바일/저사양 환경에서 수 초의 블로킹을 유발하므로 **불채택**. 대신 Python 표준 라이브러리(`csv`, `json`)만으로 빌드 스크립트를 작성해 정적 JSON을 리포에 커밋한다. (GitHub Pages는 서버 로직이 없으므로 JSON을 커밋해야 함)

- 스크립트 경로: `scripts/build_data.py`
- 실행: `python scripts/build_data.py` → `data/summary.json`, `data/deals.json` 갱신
- 필터 조건: `gu in {강남구, 서초구, 송파구}` **AND** `deal_type == '매매'` → 9,754행

### 5.2 산출 파일 ①: `data/summary.json` (약 5KB)

차트/KPI 전용 사전 집계. 클라이언트에서 재계산 없이 바로 사용.

```json
{
  "meta": {
    "period": { "from": "2025-07", "to": "2026-06" },
    "total_deals": 9754,
    "generated_at": "2026-08-12",
    "source": "국토교통부 아파트 실거래가 (매매)"
  },
  "by_gu": {
    "강남구": {
      "count": 3412,
      "avg_price": 274300,
      "avg_price_per_pyeong": 12400,
      "max_deal": { "price": 700000, "complex": "래미안대치팰리스", "area_m2": 114.2, "contract_date": "2026-02-11" },
      "monthly": [
        { "ym": "2025-07", "count": 289, "avg_price": 268100 },
        { "ym": "2025-08", "count": 301, "avg_price": 269500 }
      ]
    },
    "서초구": { "...": "동일 스키마" },
    "송파구": { "...": "동일 스키마" }
  }
}
```
- 금액 단위는 **원본 그대로 만원**(정수). 억 변환은 클라이언트 포맷터가 담당.
- `monthly` 배열은 12개 원소 고정, 오름차순 정렬.
- 전체(3구 합산) KPI는 클라이언트에서 `by_gu` 합산으로 계산(선택 구가 가변이므로 사전 계산 불필요).

### 5.3 산출 파일 ②: `data/deals.json` (약 900KB, gzip 전송 시 ~200KB)

테이블·검색용 전체 거래 레코드 9,754건. 키 이름을 1~2글자로 축약해 용량을 절감한다.

```json
{
  "fields": ["d", "g", "n", "c", "a", "f", "p", "pp"],
  "rows": [
    ["2025-07-01", 0, "개포동", "개포자이프레지던스", 84.8, 2, 369000, 14392]
  ]
}
```
| 키 | 의미 | 원본 컬럼 | 비고 |
|---|---|---|---|
| `d` | 계약일 | `contract_date` | `YYYY-MM-DD` |
| `g` | 자치구 인덱스 | `gu` | `0=강남구, 1=서초구, 2=송파구` (문자열 반복 제거) |
| `n` | 법정동 | `dong` | |
| `c` | 단지명 | `complex` | |
| `a` | 전용면적 | `area_m2` | float |
| `f` | 층 | `floor` | int |
| `p` | 거래금액(만원) | `price` | int |
| `pp` | 평당가(만원) | `price_per_pyeong` | int |

- 배열-of-배열 구조로 키 반복을 제거(객체 배열 대비 약 60% 용량 절감).
- 결측 없음이 확인되었으므로 null 처리 로직 불필요. 단, 파서에서 `price` 파싱 실패 행은 스킵(방어).
- 정렬 상태로 저장할 필요 없음(클라이언트가 정렬).
- **파일 크기 상한 기준**: `deals.json` 1.5MB 초과 시, 상위 3,000건만 담는 방식으로 축소 재검토.

### 5.4 클라이언트 로딩 순서

1. `summary.json` 먼저 fetch → KPI/차트 즉시 렌더 (첫 화면 빠르게)
2. 병렬로 `deals.json` fetch → 완료 시 테이블 렌더 (그전까지 스켈레톤)

---

## 6. 디자인 가이드 — 밝고 깔끔한 화이트 톤

### 6.1 컬러 팔레트

| 역할 | Hex | 용도 |
|---|---|---|
| Background | `#FFFFFF` | 페이지 기본 배경 |
| Surface Alt | `#F8FAFC` | 섹션 구분 배경, 테이블 hover |
| Border | `#E2E8F0` | 카드 테두리, 구분선 |
| Text Primary | `#0F172A` | 제목, 수치 |
| Text Secondary | `#64748B` | 라벨, 보조 설명 |
| Text Tertiary | `#94A3B8` | 축 눈금, 캡션 |
| Accent (Primary) | `#2563EB` | 버튼 활성, 링크, 포커스 링 |
| Accent Hover | `#1D4ED8` | hover 상태 |

**구별 시리즈 컬러** (차트·뱃지·토글에서 일관 적용)

| 자치구 | Hex | 연한 배경(뱃지) |
|---|---|---|
| 강남구 | `#2563EB` | `#EFF6FF` |
| 서초구 | `#059669` | `#ECFDF5` |
| 송파구 | `#EA580C` | `#FFF7ED` |

- Positive `#059669` / Negative `#DC2626` (증감 표기 사용 시)

### 6.2 타이포그래피

- 폰트 스택: `'Pretendard', -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Malgun Gothic', sans-serif` (Pretendard는 CDN 로드, 실패 시 시스템 폰트 폴백)
- 숫자에는 `font-variant-numeric: tabular-nums` 적용(테이블 정렬 안정화)

| 스타일 | size / weight / line-height |
|---|---|
| Page Title (h1) | 32px / 700 / 1.3 |
| Section Title (h2) | 20px / 600 / 1.4 |
| KPI 수치 | 30px / 700 / 1.2 |
| KPI 라벨 | 13px / 500 / 1.4 (`Text Secondary`) |
| Body | 14px / 400 / 1.6 |
| Table cell | 13px / 400 / 1.5 |
| Caption / 축 라벨 | 12px / 400 / 1.4 (`Text Tertiary`) |

### 6.3 레이아웃 그리드

- 컨테이너 `max-width: 1200px`, `margin: 0 auto`, `padding: 0 24px`
- 섹션 간 상하 여백 `48px`, 섹션 제목-콘텐츠 간격 `16px`
- KPI 카드: `display:grid; grid-template-columns: repeat(4, 1fr); gap:16px`
- 비교 차트 2개: `grid-template-columns: 1fr 1fr; gap:24px`
- **반응형 브레이크포인트**
  - `≤1024px`: KPI 2열, 비교 차트 1열 스택
  - `≤640px`: KPI 1열, 컨테이너 padding 16px, 테이블은 가로 스크롤(`overflow-x:auto`) 처리

### 6.4 컴포넌트 스타일

| 컴포넌트 | 스펙 |
|---|---|
| Card | `background:#FFF; border:1px solid #E2E8F0; border-radius:12px; padding:20px 24px; box-shadow:0 1px 2px rgba(15,23,42,.04)` |
| Toggle Pill | `height:36px; padding:0 16px; border-radius:18px; font:14px/1 600;` 비활성: `border:1px solid #E2E8F0; color:#64748B; background:#FFF` / 활성: `background:{구 컬러}; color:#FFF; border-color:transparent` / `transition:.15s` |
| Search Input | `height:40px; padding:0 40px 0 14px; border:1px solid #E2E8F0; border-radius:8px; font-size:14px;` focus: `border-color:#2563EB; box-shadow:0 0 0 3px rgba(37,99,235,.12)` |
| Control Bar | `position:sticky; top:0; z-index:10; background:rgba(255,255,255,.92); backdrop-filter:blur(8px); border-bottom:1px solid #E2E8F0; padding:12px 0` |
| Chart 영역 | Card 안에 배치, 높이 — 막대차트 260px / 선그래프 320px. 그리드선 `#F1F5F9` 1px, 축선은 x축만 `#E2E8F0` |
| Table | 헤더 `background:#F8FAFC; height:44px; font:12px/1 600; color:#64748B;` sticky 헤더. 행 높이 48px, 행 구분선 `1px solid #F1F5F9`. hover `background:#F8FAFC` |
| Gu Badge | `padding:2px 8px; border-radius:6px; font:12px/1.5 600; background:{연한 배경}; color:{구 컬러}` |
| Skeleton | `background:#F1F5F9; border-radius:8px;` + 1.4s shimmer 애니메이션 |

---

## 7. 기술 스택 및 제약

| 항목 | 결정 |
|---|---|
| 호스팅 | GitHub Pages (`main` 브랜치 루트). 빌드 스텝 없음 — 커밋된 파일이 곧 배포본 |
| 마크업/스타일 | 순수 HTML5 + CSS (CSS 변수로 팔레트 토큰화, `:root`에 정의). 전처리기 없음 |
| 스크립트 | 바닐라 ES6+ (`type="module"` 사용 가능). **프레임워크(React/Vue/Svelte) 사용 금지**, npm/번들러 사용 금지 |
| 차트 라이브러리 | **CDN 방식으로 사용 허용**. 권장: `Chart.js v4` (`<script src="https://cdn.jsdelivr.net/npm/chart.js@4"></script>`). 대안으로 순수 SVG 직접 구현도 허용하나, 툴팁·반응형 구현 비용을 고려해 Chart.js 우선 |
| 폰트 | Pretendard CDN(`cdn.jsdelivr.net/gh/orioncactus/pretendard`). 로드 실패 시 시스템 폰트 폴백 필수 |
| 데이터 빌드 | Python 3 표준 라이브러리만 (`csv`, `json`). pandas 등 외부 의존성 금지 |
| 브라우저 | 최신 Chrome/Edge/Safari 기준. IE 미지원 |
| 파일 구조 | `dashboard.html`, `assets/dashboard.css`, `assets/dashboard.js`, `data/summary.json`, `data/deals.json`, `scripts/build_data.py` |
| 기존 파일 | `index.html`, `montage.html`, `analyze.py`는 **수정하지 않음**. `index.html`에서 대시보드로 가는 링크 1개만 추가 |
| 원본 CSV | 27MB `seoul-apt-latest.csv`는 대시보드 런타임에서 사용하지 않음 (빌드 스크립트 입력으로만 사용) |

---

## 8. 비범위 (Out of Scope)

- 전세/월세(`deal_type != 매매`) 데이터 시각화 — 매매만 다룸
- 강남3구 외 서울 22개 자치구 확장
- 지도 기반 시각화(단지 위치 마커, 좌표 데이터 없음)
- 기간(날짜 범위) 슬라이더 필터, 면적/가격대 필터 — v1은 구 토글 + 텍스트 검색만
- 단지 상세 페이지, 개별 단지 시세 추이 차트
- 실시간 API 연동, 데이터 자동 갱신(수동 스크립트 실행 방식)
- 가격 예측·회귀 분석 등 통계 모델링
- 로그인, 즐겨찾기, 데이터 CSV 내보내기
- 다크 모드 (이번 버전은 화이트 톤 단일 테마)
- 다국어(i18n), URL 쿼리스트링에 필터 상태 저장

---

## 9. 성공 기준

1. **성능**: 초기 로드 후 KPI·차트가 **1.5초 이내** 렌더링되고, 구 토글·검색·정렬 조작 시 **200ms 이내** 화면이 갱신된다. `deals.json` 전송 크기 300KB(gzip) 이하.
2. **인터랙션 정합성**: 구를 토글하면 KPI·비교 차트·추이 차트·테이블 4개 영역이 **하나도 빠짐없이** 동시에 갱신되고, 검색 결과 0건·단일 구 선택 등 엣지 케이스에서 깨지지 않는다.
3. **데이터 정확성**: 화면의 총 거래량 합계가 원본 CSV의 강남3구 매매 9,754건과 일치하고, KPI 평균가가 `analyze.py` 등 독립 계산 결과와 오차 없이 맞는다.
4. **디자인 완성도**: §6 팔레트/타이포/간격 스펙을 준수하고, 1280px·768px·375px 세 폭에서 가로 스크롤이나 요소 겹침 없이 보인다.
5. **배포 재현성**: GitHub Pages URL을 처음 방문한 사람이 별도 설명 없이 데이터 기간·범위·조작 방법을 이해할 수 있고, 콘솔 에러 0건이다.
